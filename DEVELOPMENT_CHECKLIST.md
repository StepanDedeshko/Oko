# Чеклист разработки

## После изменения

```bash
python3 -m py_compile main.py
find app -name "*.py" -print0 | xargs -0 -n1 python3 -m py_compile
```

Проверить вручную:

- запуск `./run_oko.sh`;
- окно авторизации;
- заставка;
- Главная открывается по клику на логотип/название;
- список продуктов не содержит Главную;
- выбор темы на Главной;
- профиль с Zabbix/ОТРС доступами;
- режим дежурства;
- открытие графиков;
- `CHECK_SYSTEM_DEPS.sh`.
