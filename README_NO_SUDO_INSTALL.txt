NO SUDO INSTALL

В этой сборке:
- INSTALL_OKO_GUI.sh не запускает sudo;
- INSTALL_OKO.sh не запускает sudo;
- run_oko.sh не запускает sudo.

Если не хватает системных пакетов, будет показана команда для администратора.
