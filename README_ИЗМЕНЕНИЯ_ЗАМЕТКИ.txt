ИЗМЕНЕНИЯ:

- Убраны всплывающие окна после копирования/автозаполнения заметки.
- Кнопка "Есть проблема" убрана.
- Кнопка "Успешная проверка" переименована в "Проверка выполнена".
- При "Проверка выполнена" создаётся заметка со стандартным текстом:
  Показатели в пределах нормы. Отклонений не обнаружено.

Предупреждение в терминале:
Fontconfig error: Cannot load default config file: No such file: (null)

Обычно не критично для работы приложения. Это предупреждение библиотеки шрифтов Linux/Qt.
