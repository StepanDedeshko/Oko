СЧЕТЧИК ПРОБЛЕМ БЕЗ ЛИШНЕЙ КНОПКИ

Кнопка "Обновить счётчик" убрана.

Теперь счётчик работает автоматически:
1. При открытии страницы "Проблемы".
2. После нажатия общей кнопки "Обновить".
3. После автоперезагрузки страницы.
4. С повторными попытками, если Zabbix дорисовывает таблицу не сразу.

Логика осталась та же:
selector = ".table-stats"
regex = "из\\s+(\\d+)\\s+найден"

Для строки:
<div class="table-stats">Отображено с 1 до 100 из 294 найденных</div>

должно показывать:
Всего проблем: 294
